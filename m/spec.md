Elos `m` Spec
-------------

### `m <subcommand>`

where `subcommand` is one of:

 - `imap`
 - `goog`

#### `m imap <action> <resource>`

where action is one of: 

 - `get`

and resource is one of:

 - `mailbox[es] (mb)`
 - `


